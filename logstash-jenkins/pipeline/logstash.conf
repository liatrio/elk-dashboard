input { 
  tcp { 
    port => 9000 
    codec => json
  } 
} 

output { 
  elasticsearch { 
    index => "build" 
    document_id => "%{[@metadata][id]}" 
    hosts => ["http://a24754ee.ngrok.io:80"] } 
  stdout { codec => rubydebug } 
}
